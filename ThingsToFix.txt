Current Issues:
- Should we clamp phi to 0.1 or 0 in the thrust model.
        Model is valid between 0.1 to 1.2. At 0.1 it gives positive thrust,
        but might be more realistic to let it go to zero so get engine drag?
- Decide if the controller approach i've stubbed out makes sense
- Decide if we use the papers exponential density model or we use ISA
    (e.g. if we approximate density around a trim point or not)
- Check if my interpretation of the gravimetrics units being scaled by vehicle length
is correct.
- Check if the way i neglected qBar and phi impact on beta coefficients for thrust
model is correct - the paper seemed to suggest this appraoch was fine, but hard to tell
- Check if my units for thrust are correct? could be missing a scale factor of g
for the beta coefficients? Units very unclear.

Improvements/Depend on Controller 
- Potentially convert the controllers to a basic handler class and then
you can easily save the internal control states and extract after the simulation
has ran

Things to Check:
- Need to check my implementation of aero and thrust and dynamics has no errors